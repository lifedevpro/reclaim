# Applications

This directory contains the executable RECLAIM applications.

- `web/` — web/mobile-first frontend application
- `api/` — backend API application

The concrete framework and implementation structure will follow the approved RECLAIM Foundation specification.