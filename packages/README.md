# Shared Packages

Reusable libraries and shared code for RECLAIM live here.

Examples may include shared types, validation schemas, UI primitives, API contracts, and configuration packages once the Foundation specification defines them.